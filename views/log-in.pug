//- extends index.pug
include includes/utils.pug

block logIn
  .form-container
    h2 Log In
    div(class='errors-container')
    form(class="logInForm")
      input(type="hidden" name="_csrf" value=csrfToken)
      input(name='email' class='emailLogIn' type='email' placeholder='Enter Email')
      input(name='password' class='passwordLogIn' type='password' placeholder='Enter Password')
      button.btn.btn-primary(type='submit') Log In
    //- button.demo.btn.btn-primary(type='submit') Demo Login
    p Don't have an account?
      a.swapToSignUp  Sign up here.
